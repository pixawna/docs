<div class="content-panel">
  <div class="centered-content"><slot /></div>
</div>

<style>
  @layer starlight.components {
    .content-panel {
      padding: 1rem var(--sl-content-pad-x) 1.5rem;
    }

    .content-panel + .content-panel {
      border-top: 1px solid var(--sl-color-hairline);
    }

    .centered-content {
      width: 100%;
      max-width: var(--sl-content-width);
      margin-inline: auto;
    }

    .centered-content > :global(* + *) {
      margin-top: 1.5rem;
    }

    .centered-content > :global(:first-child) {
      margin-top: 0 !important;
    }

    @media (max-width: 40rem) {
      .content-panel {
        padding-block: 1rem;
      }

      .centered-content > :global(* + *) {
        margin-top: 1.25rem;
      }
    }
  }
</style>
